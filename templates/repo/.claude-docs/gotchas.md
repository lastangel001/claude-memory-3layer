---
tags: [memory/repo, gotchas]
---

# Gotchas

Things that look right but break, or look wrong but are intentional. Read this before reaching for a fix.

<!-- Each entry:
     ## Short heading describing the gotcha
     confidence: verified | inferred   <- verified = reproduced/ran it; inferred = deduced from reading
     1-3 paragraphs explaining what's confusing and what to do.
     Cite a commit/PR/file:line where useful.

     Fact lifecycle — never silently delete or rewrite an entry:
     - Entry no longer true -> add `status: superseded` under its heading and move
       the old claim to a dated line in `## History` at the bottom of the file
       (`- 2026-03→06: <heading> — was X, now Y`).
     - Top of file = current truth. History = the trail.

     The HIGHEST-LEVERAGE memory in this whole system. Future you (or a teammate,
     or a server agent) will hit the same wall — leave a one-paragraph warning. -->

## (no gotchas captured yet — populate as you discover)
